@model IEnumerable<BanDongHo.Models.Category>
<ul class="nav nav-pills nav-stacked">
    @foreach (var item in Model)
    {
        <li><a href="~/san-pham/@item.Slug"> <span class="pull-right">(50)</span>@item.Name</a></li>
    }
</ul>